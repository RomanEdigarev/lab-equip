type Equipment {
    id: String!
    number: Int!
    name: String!
    model: String!
}

type Query {
    equipments: [Equipment!]!
}

type Mutation {
    deleteEquipments(id: String!): Equipment!
}
