type Viewer {
    id: ID,
    token: String,
    avatar: String,
    hasWallet: Boolean,
    didRequest: Boolean!
}

input LogInInput {
    code: String!
}

type Query {
    auth: String!
}



type Mutation {
    logIn(input: LogInInput): Viewer!,
    logOut: Viewer!
}
